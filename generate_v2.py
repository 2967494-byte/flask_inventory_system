"""
Финальный генератор товаров v2.
- Реалистичные цены для каждой категории
- Генерация стабильных изображений через Lorem Picsum
- Умный подбор цен
"""
import random
import uuid
import os
import requests
from datetime import datetime, timedelta
from app import create_app, db
from app.models import Product, Category, User, Region

import sys

# Настройки по умолчанию
DEFAULT_COUNT = 50
OWNER_USER_ID = 1

COMPANY_NAMES = [
    'ООО "СтройТехСнаб"', 'АО "ПромКомплект"', 'ООО "ИндустриТорг"',
    'ЗАО "ТехноСнаб"', 'ООО "МетизПром"', 'АО "ЭлектроКомплект"',
]

# Структура: Категория -> [(Название, Мин.цена, Макс.цена)]
PRODUCTS_DB = {
    # 1. Электрооборудование
    "Электрооборудование": [
        ("Щит распределительный навесной ЩРн-П-6 IP41", 3000, 8000),
        ("Трансформатор ТМГ-1000/10/0.4", 400000, 650000),
        ("Автоматический выключатель ВА47-29 3P 25А", 150, 450),
        ("Кабель ВВГнг-LS 3х2.5 мм² (бухта 100м)", 6000, 9000),
        ("Светильник LED ДПП 01-135-50-Д120", 1500, 3500),
        ("Контактор КМИ-23210 32А 230В", 800, 1500),
        ("Электродвигатель АИР 132 М4 11кВт", 25000, 40000),
        ("Частотный преобразователь 5.5кВт 380В", 12000, 20000),
        ("Розетка силовая 32А 380В", 400, 800),
        ("Муфта кабельная концевая 4КВТп-1", 1200, 2500),
    ],
    # 2. Трубопроводная арматура
    "Трубопроводная арматура": [
        ("Задвижка клиновая 30с41нж Ду100 Ру16", 15000, 25000),
        ("Кран шаровой фланцевый КШФР Ду50 Ру40", 3500, 6000),
        ("Затвор дисковый поворотный Ду200 Ру16", 8000, 14000),
        ("Клапан обратный 16ч6р Ду80", 2500, 4500),
        ("Фланец воротниковый Ду100 Ру16", 800, 1500),
        ("Отвод стальной 90гр 219х6", 3000, 5000),
        ("Конденсатоотводчик термодинамический", 4000, 9000),
        ("Вентиль запорный 15кч18п Ду40", 600, 1200),
    ],
    # 3. Метизы и крепеж
    "Метизы и крепеж": [
        ("Болт М16х60 ГОСТ 7798-70 оцинкованный (кг)", 150, 300),
        ("Гайка М12 DIN 934 класс прочности 8 (кг)", 200, 400),
        ("Шайба 20 ГОСТ 11371-78 увеличенная (кг)", 150, 250),
        ("Шпилька М20х1000 DIN 975", 300, 600),
        ("Анкер клиновой 12х100 (уп. 50 шт)", 1500, 2500),
        ("Саморезы кровельные 4.8х35 (уп. 250 шт)", 600, 900),
    ],
    # 4. Насосное оборудование
    "Насосное оборудование": [
        ("Насос центробежный К 65-50-160", 45000, 75000),
        ("Насос погружной ЭЦВ 6-10-110", 35000, 60000),
        ("Насосная станция Grundfos Hydro MPC-E", 250000, 450000),
        ("Насос вихревой ВК 1/16 0.37кВт", 12000, 18000),
        ("Мотопомпа для грязной воды 80м3/ч", 25000, 40000),
        ("Насос дозировочный НД 2.5", 30000, 55000),
    ],
    # 5. Подшипники
    "Подшипники": [
        ("Подшипник 6205-2RS (25х52х15)", 150, 400),
        ("Подшипник роликовый 32210 (50х90х23)", 800, 1500),
        ("Подшипник шариковый 180204 (20х47х14)", 200, 500),
        ("Подшипник корпусной UCP 208", 600, 1200),
        ("Подшипник сферический 22312", 2500, 4500),
    ],
    # 6. Инструмент
    "Инструмент": [
        ("Дрель ударная Makita HP1631K 710Вт", 4500, 7000),
        ("Болгарка Bosch GWS 750-125 750Вт", 5000, 8000),
        ("Перфоратор DeWalt D25133K 800Вт SDS-plus", 12000, 18000),
        ("Компрессор воздушный 50л 8атм", 15000, 22000),
        ("Набор ключей рожковых 6-32мм", 2500, 4000),
        ("Сварочный инвертор Ресанта САИ-220", 8000, 12000),
        ("Шуруповерт аккумуляторный 18В Li-Ion", 6000, 10000),
    ],
    # 7. Спецтехника
    "Спецтехника": [
        ("Погрузчик фронтальный SEM 655D 2017г.в.", 3500000, 5500000),
        ("Экскаватор гусеничный Hyundai R220LC-9S", 6500000, 9000000),
        ("Автокран Галичанин 25т (КамАЗ)", 8000000, 12000000),
        ("Мини-погрузчик Bobcat S530", 2500000, 4000000),
        ("Вилочный погрузчик Toyota 1.5т (дизель)", 1200000, 1800000),
        ("Гидромолот Delta F-10", 350000, 500000),
    ],
    # 8. Строительные материалы
    "Строительные материалы": [
        ("КИРПИЧ ОГНЕУПОР.ОСН:ФАСОННЫЙ;ША К-713-13", 85, 150),
        ("Цемент ПЦ 500-Д0 М500 50кг (паллет)", 15000, 20000),
        ("Пескобетон М300 (40кг)", 250, 400),
        ("Профнастил С8 оцинкованный 0.45мм", 400, 600),
        ("Утеплитель Минвата 50мм (упаковка)", 800, 1500),
        ("Гипсокартон влагостойкий 12.5мм", 350, 500),
        ("Сетка кладочная 50х50х3", 100, 200),
    ],
    # 9. Оргтехника и компьютеры
    "Оргтехника": [
        ("МФУ лазерное Kyocera ECOSYS M2040dn", 35000, 55000),
        ("Принтер этикеток Godex G500", 12000, 18000),
        ("Монитор 24\" IPS FullHD", 10000, 15000),
        ("Серверный шкаф 42U 600x1000", 25000, 40000),
        ("ИБП APC Smart-UPS 1000VA", 30000, 50000),
        ("Ноутбук рабочий i5/8Gb/SSD256", 40000, 60000),
    ],
    # 10. Спецодежда и СИЗ
    "Спецодежда": [
        ("Костюм рабочий летний (куртка+п/комб)", 1500, 3000),
        ("Ботинки рабочие с металлоноском", 2000, 5000),
        ("Перчатки нитриловые (уп. 100 пар)", 800, 1500),
        ("Каска строительная защитная", 300, 800),
        ("Жилет сигнальный светоотражающий", 200, 500),
        ("Маска сварщика хамелеон", 1500, 4000),
    ],
    # 11. Металлопрокат
    "Металлопрокат": [
        ("Труба профильная 40х20х1.5 (тонна)", 65000, 85000),
        ("Арматура рифленая А500С ф12 (тонна)", 60000, 75000),
        ("Лист стальной г/к 3мм (тонна)", 70000, 90000),
        ("Швеллер 10П (тонна)", 75000, 95000),
        ("Уголок стальной 50х50х5 (тонна)", 68000, 88000),
    ],
    # 12. Грузоподъемное оборудование
    "Грузоподъемное": [
        ("Таль электрическая канатная 2т 6м", 45000, 70000),
        ("Строп текстильный ленточный 3т 4м", 800, 1500),
        ("Домкрат гидравлический бутылочный 20т", 2500, 4500),
        ("Лебедка электрическая тяговая", 15000, 30000),
        ("Тележка гидравлическая (рохля) 2.5т", 20000, 30000),
    ],
    # 13. Химическая продукция
    "Химическая продукция": [
        ("Тосол А-40М (бочка 200л)", 15000, 25000),
        ("Растворитель 646 (канистра 10л)", 1000, 1800),
        ("Кислота серная техническая 43%", 50, 100),
        ("Сода кальцинированная (мешок 25кг)", 800, 1500),
        ("Смола эпоксидная ЭД-20 (барабан 50кг)", 25000, 40000),
    ]
}

def download_placeholder_image(upload_dir, seed_str):
    """Скачивает стабильную картинку с Picsum на основе названия"""
    try:
        # Генерируем уникальный, но стабильный ID для картинки (0-1000)
        img_id = abs(hash(seed_str)) % 1000
        url = f"https://picsum.photos/id/{img_id}/800/600"
        
        response = requests.get(url, timeout=10, allow_redirects=True)
        if response.status_code == 200:
            filename = f"{uuid.uuid4().hex}.jpg"
            filepath = os.path.join(upload_dir, filename)
            with open(filepath, 'wb') as f:
                f.write(response.content)
            return filename
    except:
        pass
    return None

def main():
    # Чтение аргументов
    count = DEFAULT_COUNT
    if len(sys.argv) > 1:
        try:
            count = int(sys.argv[1])
        except ValueError:
            print("❌ Ошибка: Укажите корректное число товаров")
            return

    app = create_app()
    with app.app_context():
        # Сброс сессии
        db.session.remove()
        
        upload_dir = os.path.join(app.root_path, 'static/uploads')
        os.makedirs(upload_dir, exist_ok=True)
        
        owner = db.session.get(User, OWNER_USER_ID)
        categories = Category.query.all()
        regions = Region.query.filter_by(parent_id=None).all()
        
        print(f"🚀 Генерация {count} товаров с РЕАЛЬНЫМИ ценами...")
        
        cnt = 0
        
        # 1. Формируем маппинг "Категория БД" -> "Список подходящих товаров"
        print("📊 Анализ категорий...")
        category_map = {}
        
        # Общие (generic) товары на случай, если категория совсем непонятная
        generic_items = [
            ("Паллет деревянный 1200х800", 150, 300),
            ("Коробка картонная 600х400х400", 20, 50),
            ("Стрейч-пленка 500мм 20мкм", 300, 500),
        ]
        
        for cat in categories:
            matched_items = []
            
            # Ищем совпадение по имени категории в нашем словаре
            # Перебор всех ключей словаря PRODUCTS_DB
            for dict_cat_name, items in PRODUCTS_DB.items():
                # Простая проверка вхождения строк друг в друга (case-insensitive)
                if dict_cat_name.lower() in cat.name.lower() or cat.name.lower() in dict_cat_name.lower():
                    matched_items.extend(items)
            
            # Дополнительные ключевые слова для маппинга
            if not matched_items:
                if 'строй' in cat.name.lower() or 'ремонт' in cat.name.lower():
                   matched_items.extend(PRODUCTS_DB.get("Строительные материалы", []))
                elif 'компьютер' in cat.name.lower() or 'it' in cat.name.lower():
                   matched_items.extend(PRODUCTS_DB.get("Оргтехника", []))
                elif 'станок' in cat.name.lower() or 'оборудование' in cat.name.lower():
                   matched_items.extend(PRODUCTS_DB.get("Инструмент", []) + PRODUCTS_DB.get("Спецтехника", []))
            
            # Если так и не нашли - используем generic
            if not matched_items:
                category_map[cat.id] = generic_items
            else:
                category_map[cat.id] = matched_items

        print(f"🚀 Генерация {count} товаров с УМНЫМ распределением...")
        
        cnt = 0
        for i in range(count):
            try:
                # 1. Выбираем случайную категорию из БД
                cat_obj = random.choice(categories)
                
                # 2. Берем строго подходящие ей товары
                candidates = category_map.get(cat_obj.id)
                if not candidates:
                    candidates = generic_items
                
                item_data = random.choice(candidates)
                
                if len(item_data) == 3:
                     title, min_p, max_p = item_data
                     price = round(random.uniform(min_p, max_p), 2)
                else:
                    title = item_data[0]
                    price = 1000
                
                # 3. Картинка (одна)
                images = []
                img_file = download_placeholder_image(upload_dir, title)
                if img_file:
                    images.append(img_file)

                # 4. Создаем продукт
                product = Product(
                    title=title,
                    description=f"Состояние: Новое\n\nПредлагаем: {title}.\nЦена указана с НДС.\nДоставка по РФ.",
                    price=price,
                    price_type="fixed",
                    quantity=random.randint(10, 5000) if "Болт" in title else random.randint(1, 50),
                    category_id=cat_obj.id,
                    user_id=owner.id,
                    status=1,
                    images=images,
                    condition="new",
                    region=random.choice(regions).name if regions else None,
                    region_id=random.choice(regions).id if regions else None,
                    external_organization=random.choice(COMPANY_NAMES),
                    external_contact="Отдел продаж",
                    external_phone="+X XXX XXX-XX-XX",
                    vat_included=True,
                    source_url=None,
                    view_count=random.randint(0, 300),
                    created_at=datetime.now() - timedelta(days=random.randint(1, 45))
                )
                
                db.session.add(product)
                cnt += 1
                
                # Коммит пачками по 10
                if cnt % 10 == 0:
                    db.session.commit()
                    print(f"  Создано {cnt}...")

            except Exception as e:
                print(f"⚠️ Ошибка на {i}: {e}")
                db.session.rollback()
        
        db.session.commit()
        print(f"✅ Успешно! Создано {cnt} товаров.")

if __name__ == "__main__":
    main()
